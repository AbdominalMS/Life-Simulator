const add = (num1, num2) => {
   let parsed1 = parseInt(num1)
   let parsed2 = parseInt(num2)
    let answer = parsed1 + parsed2
    return answer
}
module.exports.Add = add
const subtract = (num1, num2) => {
    let parsed1 = parseInt(num1)
    let parsed2 = parseInt(num2)
     let answer = parsed1 - parsed2
     return answer
 }
module.exports.Subtract = subtract
const getrandomfromarray = (array) => {
    const index = Math.floor(Math.random() * array.length);
     return array[index]
 }
 module.exports.randArray = getrandomfromarray
 const randgen = (min, max) => {
    let answer = Math.floor(Math.random() * (max - min) + min)
    return answe
 }
 module.exports.createRand = randgen